import 'package:flutter/material.dart';
class AdminDashboard extends StatelessWidget{
const AdminDashboard({super.key});
@override
Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Admin Dashboard')),body:const Center(child:Text('Station management, users, bookings and reports coming next.')));
}